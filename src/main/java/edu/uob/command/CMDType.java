package edu.uob.command;

public enum CMDType {
    INSERT,
    SELECT,
    USE,
    CREATE,
    CREATEDATABASE,
    CREATETABLE,
    DROP,
    DROPDATABASE,
    DROPTABLE,
    JOIN,
    ALTER,
    UPDATE,
    DELETE
}
